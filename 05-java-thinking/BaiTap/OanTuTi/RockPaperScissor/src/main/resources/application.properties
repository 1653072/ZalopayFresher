# Config database information
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/rps?useUnicode=true&CharacterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=

# Define port 8000
server.port=8000

# Define secret for JWT
jwt.secret=quoctk08

# Hibernate is assuming that the entity field "returnPolicy" corresponds to the table column "return_policy". 
# But actually, the column name is "returnPolicy" => We make Hibernate cann't change our variable name. 
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
